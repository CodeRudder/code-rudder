---
description: Start the code-rudder
---

# Rudder Command

## Usage

`/start`

## Action

- `start` → Execute `node -e "const path = require('path'); const os = require('os'); require(path.join(os.homedir(), '.claude', 'plugins', 'marketplaces', 'code-rudder', 'scripts', 'start.js'))"`
