---
description: Stop the code-rudder
---

# Rudder Command

## Usage
`/stop`

## Action
Execute `node -e "const path = require('path'); const os = require('os'); require(path.join(os.homedir(), '.claude', 'plugins', 'marketplaces', 'code-rudder', 'scripts', 'stop.js'))"`
